local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()

local Window = Fluent:CreateWindow({
    Title = "🍎 Apple Hub | V5 Banana Killer (GOD)",
    SubTitle = "By Quoc Hoa",
    TabWidth = 180, Size = UDim2.fromOffset(600, 480), Acrylic = true, Theme = "Dark",
    MinimizeKey = Enum.KeyCode.LeftControl 
})

-- [[ HỆ THỐNG BIẾN ĐIỀU KHIỂN ]]
_G.AutoFarm = false
_G.AutoChest = false
_G.BringMob = true
_G.FastAttack = true
_G.AutoStats = false
_G.StatPoint = "Melee"
_G.SelectWeapon = "Melee"
_G.AutoRaid = false
_G.InfiniteDash = false
_G.AutoMaterial = false
_G.SelectMaterial = ""

-- [[ TỔNG HỢP CÁC TAB - THEO GIAO DIỆN MỚI ]]
local Tabs = {
    Home = Window:AddTab({ Title = "🏠 Tab Home", Icon = "home" }),
    Farming = Window:AddTab({ Title = "⚔️ Tab Farming", Icon = "shovels" }),
    Setting = Window:AddTab({ Title = "⚙️ Tab Setting", Icon = "settings" }),
    Fishing = Window:AddTab({ Title = "🎣 Tab Fishing", Icon = "fish" }),
    QuestItem = Window:AddTab({ Title = "📋 Tab Quest And Item", Icon = "clipboard-list" }),
    SeaEvent = Window:AddTab({ Title = "🌊 Tab Sea Event", Icon = "waves" }),
    MirageRace = Window:AddTab({ Title = "🧭 Tab Mirage And Race", Icon = "compass" }),
    StatsEsp = Window:AddTab({ Title = "📊 Tab Stats And Esp", Icon = "bar-chart" }),
    FruitRaid = Window:AddTab({ Title = "🍎 Tab Fruit And Raid", Icon = "zap" })
}

-- ================================
-- TAB FARMING (GIAO DIỆN GIỐNG ẢNH)
-- ================================

-- Search Section
Tabs.Farming:AddParagraph({
    Title = "🔍 Search section or Function",
    Content = "__________________________________________________"
})

-- Search Box giả lập (Fluent không có search native, tạo bằng cách thêm input)
local SearchInput = Tabs.Farming:AddInput("SearchInput", {
    Title = "Tìm kiếm tính năng",
    Default = "",
    Placeholder = "Nhập tên tính năng...",
    Callback = function(Value)
        -- Hàm tìm kiếm demo
        if Value ~= "" then
            Fluent:Notify({
                Title = "🔎 Tìm kiếm",
                Content = "Đang tìm: " .. Value,
                Duration = 2
            })
        end
    end
})

-- Các Tab nhỏ (dùng Paragraph để tạo danh sách tab)
Tabs.Farming:AddParagraph({
    Title = "📑 Danh sách Tab",
    Content = "🏠 Tab Home  |  ⚔️ Tab Farming  |  ⚙️ Tab Setting  |  🎣 Tab Fishing  |  📋 Tab Quest And Item  |  🌊 Tab Sea Event  |  🧭 Tab Mirage And Race  |  🌋 Tab Volcano Event  |  📊 Tab Stats And Esp  |  🍎 Tab Fruit And Raid"
})

Tabs.Farming:AddSection("⚙️ Main Settings")

-- Select Weapon - Dạng Dropdown
local WeaponDropdown = Tabs.Farming:AddDropdown("WeaponSelectUI", {
    Title = "Select Weapon:",
    Values = {"Melee", "Sword", "Blox Fruit"},
    Default = "Melee",
    Callback = function(Value)
        _G.SelectWeapon = Value
        Fluent:Notify({
            Title = "⚔️ Vũ khí",
            Content = "Đã chọn: " .. Value,
            Duration = 1
        })
    end
})

-- Select Fast Delay Last Attack - Dạng Dropdown
local DelayDropdown = Tabs.Farming:AddDropdown("DelaySelectUI", {
    Title = "Select Fast Delay Last Attack:",
    Values = {"0.1s", "0.2s", "0.3s", "0.5s", "0.7s", "1s"},
    Default = "0.2s",
    Callback = function(Value)
        local delayTime = tonumber(Value:sub(1, -2)) or 0.2
        Fluent:Notify({
            Title = "⏱️ Delay",
            Content = "Đã chọn: " .. Value,
            Duration = 1
        })
    end
})

Tabs.Farming:AddSection("🤖 Auto Farm Level")

-- Auto Farm Nearest
local AutoFarmToggle = Tabs.Farming:AddToggle("AutoFarmNearest", {
    Title = "Auto Farm Nearest",
    Default = false
})
AutoFarmToggle:OnChanged(function()
    _G.AutoFarm = AutoFarmToggle.Value
    if _G.AutoFarm then
        Fluent:Notify({
            Title = "🤖 Auto Farm",
            Content = "Đã bật Auto Farm Nearest",
            Duration = 2
        })
    else
        Fluent:Notify({
            Title = "🤖 Auto Farm",
            Content = "Đã tắt Auto Farm",
            Duration = 2
        })
    end
end)

-- Auto Factory Raid
local FactoryToggle = Tabs.Farming:AddToggle("AutoFactoryRaid", {
    Title = "Auto Factory Raid",
    Default = false
})
FactoryToggle:OnChanged(function()
    if FactoryToggle.Value then
        Fluent:Notify({
            Title = "🏭 Factory Raid",
            Content = "Đã bật Auto Factory Raid",
            Duration = 2
        })
    end
end)

-- Auto Pirate Raid
local PirateToggle = Tabs.Farming:AddToggle("AutoPirateRaid", {
    Title = "Auto Pirate Raid",
    Default = false
})
PirateToggle:OnChanged(function()
    if PirateToggle.Value then
        Fluent:Notify({
            Title = "🏴‍☠️ Pirate Raid",
            Content = "Đã bật Auto Pirate Raid",
            Duration = 2
        })
    end
end)

-- Thêm các chức năng khác từ bản gốc
Tabs.Farming:AddSection("📦 Extra Farm")

local ChestToggle = Tabs.Farming:AddToggle("AutoChest", {
    Title = "Auto Farm Rương",
    Default = false
})
ChestToggle:OnChanged(function()
    _G.AutoChest = ChestToggle.Value
end)

-- ================================
-- TAB QUEST AND ITEM (NGUYÊN LIỆU)
-- ================================
Tabs.QuestItem:AddSection("🌾 Farm Material")

Tabs.QuestItem:AddDropdown("SelectMaterial", {
    Title = "Chọn Nguyên Liệu",
    Values = {
        "Magma Ore (Sea 1)", "Leather (Sea 1)", "Fish Tail (Sea 1)", 
        "Mystic Droplet (Sea 2)", "Vampire Fang (Sea 2)", "Radioactive Material (Sea 2)",
        "Dragon Scale (Sea 3)", "Conjured Cocoa (Sea 3)", "Bones (Sea 3)"
    },
    Default = "Bones (Sea 3)",
    Callback = function(Value)
        _G.SelectMaterial = Value
        Fluent:Notify({
            Title = "📦 Nguyên liệu",
            Content = "Đã chọn: " .. Value,
            Duration = 2
        })
    end
})

local MatToggle = Tabs.QuestItem:AddToggle("AutoMat", {
    Title = "Auto Farm Nguyên Liệu",
    Default = false
})
MatToggle:OnChanged(function()
    _G.AutoMaterial = MatToggle.Value
end)

-- ================================
-- TAB SETTING
-- ================================
Tabs.Setting:AddSection("⚡ Player Settings")

local DashToggle = Tabs.Setting:AddToggle("InfDash", {
    Title = "Vô Hạn Dash (Lướt)",
    Default = false
})
DashToggle:OnChanged(function()
    _G.InfiniteDash = DashToggle.Value
end)

Tabs.Setting:AddSlider("WalkSpeed", {
    Title = "Tốc Độ Chạy",
    Default = 20,
    Min = 20,
    Max = 300,
    Rounding = 0,
    Callback = function(V)
        if game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("Humanoid") then
            game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = V
        end
    end
})

Tabs.Setting:AddButton({
    Title = "🔄 Server Hop",
    Callback = function()
        game:GetService("TeleportService"):Teleport(game.PlaceId, game.Players.LocalPlayer)
    end
})

-- ================================
-- TAB FRUIT AND RAID
-- ================================
Tabs.FruitRaid:AddSection("🍎 Raid & Fruit")

local RaidToggle = Tabs.FruitRaid:AddToggle("AutoRaid", {
    Title = "Auto Start & Kill Raid",
    Default = false
})
RaidToggle:OnChanged(function()
    _G.AutoRaid = RaidToggle.Value
end)

Tabs.FruitRaid:AddButton({
    Title = "🍎 Auto Nhặt Fruit",
    Callback = function()
        for i, v in pairs(game:GetService("Workspace"):GetChildren()) do
            if v:IsA("Tool") and v.Name:find("Fruit") then
                game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = v.Handle.CFrame
                task.wait(0.1)
            end
        end
        Fluent:Notify({
            Title = "🍎 Fruit",
            Content = "Đã nhặt xong các fruit gần nhất!",
            Duration = 2
        })
    end
})

-- ================================
-- TAB COMBAT (Hệ thống gom quái)
-- ================================
local CombatTab = Window:AddTab({ Title = "⚔️ Combat Tools", Icon = "swords" })
CombatTab:AddSection("🎯 Combat Settings")

local BringMobToggle = CombatTab:AddToggle("BringMob", {
    Title = "Gom Quái (Bring Mob)",
    Default = true
})
BringMobToggle:OnChanged(function()
    _G.BringMob = BringMobToggle.Value
end)

-- Thêm Fast Attack vào Combat Tab
local FastAttackToggleCombat = CombatTab:AddToggle("FastAttack", {
    Title = "Fast Attack (Đánh nhanh)",
    Default = true
})
FastAttackToggleCombat:OnChanged(function()
    _G.FastAttack = FastAttackToggleCombat.Value
end)

-- ================================
-- TAB VOLCANO EVENT (Thêm mới)
-- ================================
local VolcanoTab = Window:AddTab({ Title = "🌋 Tab Volcano Event", Icon = "flame" })
VolcanoTab:AddSection("🌋 Volcano Event Settings")
VolcanoTab:AddButton({
    Title = "Teleport to Volcano",
    Callback = function()
        Fluent:Notify({
            Title = "🌋 Volcano",
            Content = "Đang tìm kiếm vị trí núi lửa...",
            Duration = 2
        })
    end
})

-- ================================
-- HỆ THỐNG LOGIC VẬN HÀNH - GIỮ NGUYÊN
-- ================================

-- Luồng Auto Farm & Material
task.spawn(function()
    while true do
        task.wait()
        if _G.AutoFarm or _G.AutoMaterial then
            pcall(function()
                game:GetService("VirtualUser"):CaptureController()
                game:GetService("VirtualUser"):ClickButton1(Vector2.new(50, 50))
                
                if _G.BringMob then
                    local enemies = game:GetService("Workspace").Enemies:GetChildren()
                    for _, v in pairs(enemies) do
                        if not (_G.AutoFarm or _G.AutoMaterial) then break end
                        if v:FindFirstChild("Humanoid") and v.Humanoid.Health > 0 and v:FindFirstChild("HumanoidRootPart") then
                            v.HumanoidRootPart.CanCollide = false
                            v.HumanoidRootPart.CFrame = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, -5)
                            v.HumanoidRootPart.Velocity = Vector3.new(0, 0, 0)
                        end
                    end
                end
            end)
        end
    end
end)

-- Luồng Chest
task.spawn(function()
    while true do
        task.wait(0.15)
        if _G.AutoChest then
            local chests = game:GetService("Workspace"):GetChildren()
            for i, v in pairs(chests) do
                if not _G.AutoChest then break end
                if v.Name:find("Chest") and v:IsA("BasePart") then
                    pcall(function()
                        game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = v.CFrame
                        task.wait(0.2)
                    end)
                end
            end
        end
    end
end)

-- Luồng Dash
task.spawn(function()
    while true do
        task.wait(0.1)
        if _G.InfiniteDash then
            pcall(function()
                if getrenv()._G.Characters.Dash then
                    debug.setupvalue(getrenv()._G.Characters.Dash, 3, 0)
                end
            end)
        end
    end
end)

-- Luồng Fast Attack (nếu có hàm)
task.spawn(function()
    while true do
        task.wait()
        if _G.FastAttack then
            pcall(function()
                -- Thêm logic fast attack nếu cần
            end)
        end
    end
end)

-- ================================
-- NÚT TRÒN 🍎 (GIỮ NGUYÊN)
-- ================================
local ScreenGui = Instance.new("ScreenGui", game:GetService("CoreGui"))
ScreenGui.Name = "AppleHubButton"
local ToggleButton = Instance.new("TextButton", ScreenGui)
ToggleButton.Size = UDim2.new(0, 50, 0, 50)
ToggleButton.Position = UDim2.new(0, 20, 0, 200)
ToggleButton.Text = "🍎"
ToggleButton.TextSize = 30
ToggleButton.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
ToggleButton.Draggable = true
local UICorner = Instance.new("UICorner", ToggleButton)
UICorner.CornerRadius = UDim.new(1, 0)

ToggleButton.MouseButton1Click:Connect(function()
    game:GetService("VirtualInputManager"):SendKeyEvent(true, Enum.KeyCode.LeftControl, false, game)
    task.wait(0.01)
    game:GetService("VirtualInputManager"):SendKeyEvent(false, Enum.KeyCode.LeftControl, false, game)
end)

-- ================================
-- KHỞI TẠO & THÔNG BÁO
-- ================================
Window:SelectTab(1)
Fluent:Notify({
    Title = "🍎 Apple Hub V5",
    Content = "✅ Đã cập nhật giao diện Tab Farming mới!",
    Duration = 5
})
