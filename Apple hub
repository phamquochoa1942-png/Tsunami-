-- ╔══════════════════════════════════════════════════╗
-- ║  VEXZ HUB v3.0  |  By Quoc Hoa                 ║
-- ╚══════════════════════════════════════════════════╝

-- =====================================================
-- FLUENT UI
-- =====================================================
local Fluent = loadstring(game:HttpGet(
    "https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"
))()
local SaveManager = loadstring(game:HttpGet(
    "https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Addons/SaveManager.lua"
))()
local InterfaceManager = loadstring(game:HttpGet(
    "https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Addons/InterfaceManager.lua"
))()

-- =====================================================
-- SERVICES
-- =====================================================
local Players             = game:GetService("Players")
local ReplicatedStorage   = game:GetService("ReplicatedStorage")
local RunService          = game:GetService("RunService")
local TweenService        = game:GetService("TweenService")
local VirtualInputManager = game:GetService("VirtualInputManager")
local UserInputService    = game:GetService("UserInputService")
local LocalPlayer         = Players.LocalPlayer

-- =====================================================
-- LEVEL DATA
-- =====================================================
local LevelData = {
    {1,   15,  1, "StartIsland",      "Villager1",          "Bandit"},
    {15,  30,  1, "StartIsland",      "Villager2",          "Bandit"},
    {30,  60,  1, "Jungle",           "Gorilla King",       "Gorilla"},
    {60,  90,  1, "PirateVillage",    "Pirate",             "Pirate"},
    {90,  120, 1, "Desert",           "Desert Bandit",      "Desert Bandit"},
    {120, 150, 1, "SnowIsland",       "Snow Bandit",        "Snow Bandit"},
    {150, 200, 1, "MarineBase",       "Lieutenant",         "Ensign"},
    {200, 250, 1, "Skylands",         "Sky Bandit",         "Sky Bandit"},
    {250, 300, 1, "Fountain City",    "Citizen",            "Citizen"},
    {300, 375, 1, "GraveyardIsland",  "Zombie",             "Zombie"},
    {375, 450, 1, "Thriller Bark",    "Zombie",             "Zombie"},
    {450, 500, 1, "ColossiumIsland",  "Gladiator",          "Gladiator"},
    {500, 575, 1, "Magma Village",    "Magma Ninja",        "Magma Ninja"},
    {575, 625, 1, "IronIsland",       "Dark Master",        "Dark Master"},
    {625, 700, 1, "ZombieIsland",     "Dragon Crew",        "Dragon Crew Archer"},
    {700, 775, 2, "Kingdom of Rose",  "Flower",             "Chocolate Bar Battler"},
    {775, 850, 2, "DarkArena",        "Imp",                "Imp"},
    {850, 925, 2, "Ice Side",         "Ice Cream",          "Snow Lurker"},
    {925, 1000,2, "Forgotten Island", "Forest Pirate",      "Forest Pirate"},
    {1000,1050,2, "Usopp Island",     "Military Soldier",   "Military Soldier"},
    {1050,1100,2, "OakIsland",        "Diable",             "Diable"},
    {1100,1200,2, "Dimensional Rift", "Dimensional Rift",   "Deranged Slasher"},
    {1200,1250,2, "Mini Sky",         "Sky Stalker",        "Sky Stalker"},
    {1250,1350,2, "CastleOnTheSea",   "Royal Soldier",      "Royal Soldier"},
    {1350,1425,2, "SeaOfTreat",       "Sea Soldier",        "Sea Soldier"},
    {1425,1500,2, "HotAndCold",       "Ice Cream Islander", "Ice Cream Islander"},
    {1500,1575,2, "Terrorshark",      "Ship Deckhand",      "Ship Deckhand"},
    {1575,1650,3, "Floating Turtle",  "Fishman",            "Fishman Raider"},
    {1650,1700,3, "ForestOfGiants",   "Poseidon",           "Giant Warrior"},
    {1700,1750,3, "Hydra Island",     "Marine Lieutenant",  "Marine Lieutenant"},
    {1750,1800,3, "NewWorld",         "Pirate Admin",       "Pirate Admin"},
    {1800,1875,3, "Tiki Outpost",     "Pirate Millionaire", "Pirate Millionaire"},
    {1875,1950,3, "TreasureIsland",   "Corrupt Master",     "Corrupt Master"},
    {1950,2025,3, "Haunted Castle",   "Sea Samurai",        "Reborn Skeleton"},
    {2025,2100,3, "FarEast",          "Order Leader",       "Order"},
    {2100,2175,3, "GraveIsland",      "Adventurer",         "Adventurer"},
    {2175,2275,3, "FrozenPeak",       "Snow Boss",          "Yeti"},
    {2275,2375,3, "Cursed Ship",      "Pirate Lord",        "Cursed Dual Katana User"},
    {2375,2475,3, "LegacyPort",       "Pirate Warrior",     "Pirate Warrior"},
    {2475,2575,3, "BartolomeoIsland", "Bartolomeo",         "Beautiful Pirate"},
    {2575,2650,3, "LondonIsland",     "Kaiju",              "Kaiju"},
    {2650,2700,3, "Haunted Island",   "Poltergeist",        "Poltergeist"},
    {2700,2800,3, "GhostIsland",      "Demonic Soul",       "Demonic Soul"},
}

local function getDataForLevel(lv)
    for i = #LevelData, 1, -1 do
        local d = LevelData[i]
        if lv >= d[1] then
            return {minLv=d[1],maxLv=d[2],sea=d[3],island=d[4],questNPC=d[5],mobName=d[6]}
        end
    end
    return {minLv=1,maxLv=15,sea=1,island="StartIsland",questNPC="Villager1",mobName="Bandit"}
end

local function getPlayerLevel()
    local stats = LocalPlayer:FindFirstChild("leaderstats")
    if stats then
        local lv = stats:FindFirstChild("Level") or stats:FindFirstChild("Lv")
        if lv then return tonumber(lv.Value) or 1 end
    end
    return 1
end

-- =====================================================
-- REMOTES
-- =====================================================
local AttackRemote = nil
local QuestRemote = nil

pcall(function()
    AttackRemote = ReplicatedStorage.Modules.Net:FindFirstChild("RE/RegisterHit")
end)
if not AttackRemote then
    pcall(function()
        AttackRemote = ReplicatedStorage.Modules.Net:FindFirstChild("RE/RegisterAttack")
    end)
end

pcall(function()
    local remotes = ReplicatedStorage:FindFirstChild("Remotes")
    if remotes then
        QuestRemote = remotes:FindFirstChild("QuestUpdate") or remotes:FindFirstChild("StartQuest") or remotes:FindFirstChild("Quest")
    end
end)

-- =====================================================
-- CONFIG
-- =====================================================
local Cfg = {
    scanRange    = 2000,
    attackRange  = 200,
    bringRange   = 500,
    questEvery   = 30,
}

-- =====================================================
-- STATE
-- =====================================================
local State = {
    farm         = false,
    bring        = false,
    quest        = false,
    active       = false,
    questBusy    = false,
    target       = nil,
    noFallConn   = nil,
    origGravity  = workspace.Gravity,
    origWalk     = 16,
    origJump     = 50,
}

-- =====================================================
-- UTILS
-- =====================================================
local function getChar()
    local c = LocalPlayer.Character
    if not c then return nil, nil, nil end
    local r = c:FindFirstChild("HumanoidRootPart")
    local h = c:FindFirstChildOfClass("Humanoid")
    return c, r, h
end

local function resetChar()
    State.active = false
    State.target = nil
    if State.noFallConn then
        State.noFallConn:Disconnect()
        State.noFallConn = nil
    end
    workspace.Gravity = State.origGravity
    local c, r, h = getChar()
    if not c then return end
    if h then
        h.WalkSpeed     = State.origWalk
        h.JumpPower     = State.origJump
        h.PlatformStand = false
        h.AutoRotate    = true
    end
    if r then
        r.Velocity                = Vector3.zero
        r.RotVelocity             = Vector3.zero
        r.AssemblyLinearVelocity  = Vector3.zero
        r.AssemblyAngularVelocity = Vector3.zero
    end
    for _, p in ipairs(c:GetDescendants()) do
        if p:IsA("BasePart") then p.CanCollide = true end
    end
end

local function lockChar()
    local c, r, h = getChar()
    if not c then return end
    if h then
        State.origWalk    = h.WalkSpeed
        State.origJump    = h.JumpPower
        h.WalkSpeed       = 0
        h.JumpPower       = 0
        h.PlatformStand   = true
        h.AutoRotate      = false
    end
    for _, p in ipairs(c:GetDescendants()) do
        if p:IsA("BasePart") and p.Name ~= "HumanoidRootPart" then
            p.CanCollide = false
        end
    end
end

-- =====================================================
-- NOFALL
-- =====================================================
local function startNoFall(mob)
    if State.noFallConn then
        State.noFallConn:Disconnect()
        State.noFallConn = nil
    end
    workspace.Gravity = 0
    lockChar()

    State.noFallConn = RunService.RenderStepped:Connect(function()
        if not State.active then return end
        local _, r = getChar()
        if not r or not mob or not mob.Parent then return end
        local er = mob:FindFirstChild("HumanoidRootPart")
        local eh = mob:FindFirstChildOfClass("Humanoid")
        if not er or not eh or eh.Health <= 0 then return end
        r.CFrame                  = er.CFrame * CFrame.new(0, 10, 0)
        r.Velocity                = Vector3.zero
        r.RotVelocity             = Vector3.zero
        r.AssemblyLinearVelocity  = Vector3.zero
        r.AssemblyAngularVelocity = Vector3.zero
    end)
end

-- =====================================================
-- FLY TO
-- =====================================================
local function flyTo(targetPos, speed)
    speed = speed or 150
    if State.noFallConn then
        State.noFallConn:Disconnect()
        State.noFallConn = nil
    end
    workspace.Gravity = 0
    lockChar()

    local done = false
    local conn
    conn = RunService.RenderStepped:Connect(function(dt)
        local _, r = getChar()
        if not r then conn:Disconnect(); done = true; return end
        local diff = targetPos - r.Position
        if diff.Magnitude < 3 then
            r.CFrame                  = CFrame.new(targetPos)
            r.Velocity                = Vector3.zero
            r.AssemblyLinearVelocity  = Vector3.zero
            conn:Disconnect(); done = true; return
        end
        local step = math.min(speed * dt, diff.Magnitude)
        r.CFrame                  = CFrame.new(r.Position + diff.Unit * step)
        r.Velocity                = Vector3.zero
        r.AssemblyLinearVelocity  = Vector3.zero
    end)

    local t = 0
    repeat task.wait(0.05); t += 0.05 until done or t >= 10
end

-- =====================================================
-- FIND MOB
-- =====================================================
local function findMobs(fromPos, maxDist)
    maxDist = tonumber(maxDist) or 99999
    local closest, closestDist = nil, maxDist
    local nearby = {}
    local Enemies = workspace:FindFirstChild("Enemies")
    if not Enemies then return nil, maxDist, {} end

    for _, e in ipairs(Enemies:GetChildren()) do
        if not e:IsA("Model") then continue end
        local eh = e:FindFirstChildOfClass("Humanoid")
        local er = e:FindFirstChild("HumanoidRootPart")
        if not eh or eh.Health <= 0 or not er then continue end
        local dist = (fromPos - er.Position).Magnitude
        if dist <= maxDist then
            table.insert(nearby, {mob=e, dist=dist})
            if dist < closestDist then
                closestDist = dist
                closest = e
            end
        end
    end
    return closest, closestDist, nearby
end

-- =====================================================
-- BRING MOB
-- =====================================================
local function bringMobs()
    local _, r = getChar()
    if not r then return end
    local Enemies = workspace:FindFirstChild("Enemies")
    if not Enemies then return end

    local count = 0
    for _, e in ipairs(Enemies:GetChildren()) do
        if not e:IsA("Model") then continue end
        local eh = e:FindFirstChildOfClass("Humanoid")
        local er = e:FindFirstChild("HumanoidRootPart")
        if not eh or eh.Health <= 0 or not er then continue end
        local dist = (r.Position - er.Position).Magnitude
        if dist <= Cfg.bringRange then
            pcall(function()
                er.CFrame = r.CFrame * CFrame.new(math.random(-5,5), 0, math.random(-8,-3))
                er.Velocity = Vector3.zero
                er.AssemblyLinearVelocity = Vector3.zero
            end)
            count += 1
        end
    end
end

-- =====================================================
-- AUTO QUEST
-- =====================================================
local function acceptQuest()
    if State.questBusy then return end
    State.questBusy = true

    local lv   = getPlayerLevel()
    local data = getDataForLevel(lv)

    local questNPC = nil
    local questHRP = nil

    local npcsFolder = workspace:FindFirstChild("NPCs")
    if npcsFolder then
        for _, npc in ipairs(npcsFolder:GetChildren()) do
            if npc:IsA("Model") and npc.Name:lower():find(data.questNPC:lower()) then
                questNPC = npc
                questHRP = npc:FindFirstChild("HumanoidRootPart") or npc:FindFirstChild("Head")
                break
            end
        end
    end

    if not questNPC then
        for _, v in ipairs(workspace:GetDescendants()) do
            if v:IsA("Model") and v.Name:lower():find(data.questNPC:lower()) then
                questNPC = v
                questHRP = v:FindFirstChild("HumanoidRootPart") or v:FindFirstChild("Head")
                break
            end
        end
    end

    if questHRP then
        flyTo(questHRP.Position + Vector3.new(0, 3, -5), 200)
        task.wait(0.5)
    end

    pcall(function()
        if QuestRemote then
            QuestRemote:FireServer(data.questNPC, data.island, data.mobName)
        end
    end)

    task.wait(0.5)

    if State.farm then
        if State.target and State.target.Parent then
            State.active = true
            startNoFall(State.target)
        else
            State.active = false
            State.target = nil
            workspace.Gravity = State.origGravity
        end
    end

    State.questBusy = false
end

-- =====================================================
-- ATTACK
-- =====================================================
local function attackEnemy(mob)
    if not AttackRemote then return end
    local eh = mob:FindFirstChildOfClass("Humanoid")
    local er = mob:FindFirstChild("HumanoidRootPart")
    if not eh or eh.Health <= 0 or not er then return end

    local targetPart = mob:FindFirstChild("LeftLowerArm")
        or mob:FindFirstChild("RightLowerArm")
        or mob:FindFirstChild("LeftFoot")
        or mob:FindFirstChild("RightFoot")
        or mob:FindFirstChild("Head")
        or er

    pcall(function()
        AttackRemote:FireServer(targetPart, {}, "079996f9")
    end)
end

-- =====================================================
-- MAIN FARM LOOP
-- =====================================================
task.spawn(function()
    while true do
        task.wait()
        if not State.farm or State.questBusy then
            if State.active and not State.questBusy then resetChar() end
            continue
        end

        local c, r, h = getChar()
        if not c or not r or not h or h.Health <= 0 then
            if State.active then resetChar() end
            task.wait(0.2); continue
        end

        local closest, closestDist, nearby = findMobs(r.Position, Cfg.scanRange)

        local inRange = {}
        for _, item in ipairs(nearby) do
            if item.dist <= Cfg.attackRange then
                table.insert(inRange, item.mob)
            end
        end

        if closest then
            if not State.active then
                State.active = true
                State.target = closest
                startNoFall(closest)
            end

            if State.target then
                local teh = State.target:FindFirstChildOfClass("Humanoid")
                if not teh or teh.Health <= 0 or not State.target.Parent then
                    State.target = closest
                    startNoFall(closest)
                end
            end

            for _, mob in ipairs(inRange) do
                if not State.farm then break end
                attackEnemy(mob)
            end

            VirtualInputManager:SendMouseButtonEvent(0,0,0,true,game,1)
            VirtualInputManager:SendMouseButtonEvent(0,0,0,false,game,1)
        else
            if State.active then
                resetChar()
                task.wait(0.3)
            end
            if State.farm and not State.questBusy then
                local c2, r2 = getChar()
                if r2 then
                    local nextMob, dist2 = findMobs(r2.Position, Cfg.scanRange)
                    if nextMob then
                        local er2 = nextMob:FindFirstChild("HumanoidRootPart")
                        if er2 then
                            State.active = false
                            flyTo(er2.Position + Vector3.new(0, 10, 0), 300)
                            State.active = true
                            State.target = nextMob
                            startNoFall(nextMob)
                        end
                    end
                end
            end
        end
    end
end)

-- =====================================================
-- BRING MOB LOOP
-- =====================================================
task.spawn(function()
    while true do
        task.wait(2)
        if State.farm and State.bring then bringMobs() end
    end
end)

-- =====================================================
-- QUEST LOOP
-- =====================================================
task.spawn(function()
    while true do
        task.wait(Cfg.questEvery)
        if State.farm and State.quest and not State.questBusy then
            task.spawn(acceptQuest)
        end
    end
end)

-- =====================================================
-- UI - FLUENT
-- =====================================================
local Window = Fluent:CreateWindow({
    Title       = "Vexz Hub",
    SubTitle    = "By Quoc Hoa  •  v3.0",
    TabWidth    = 160,
    Size        = UDim2.fromOffset(580, 440),
    Acrylic     = true,
    Theme       = "Pink",
    MinimizeKey = Enum.KeyCode.RightShift,
})

local Tabs = {
    Farm  = Window:AddTab({ Title = "Farm",  Icon = "swords"   }),
    Mob   = Window:AddTab({ Title = "Mob",   Icon = "zap"      }),
    Quest = Window:AddTab({ Title = "Quest", Icon = "map-pin"  }),
    Misc  = Window:AddTab({ Title = "Misc",  Icon = "settings" }),
    Info  = Window:AddTab({ Title = "Info",  Icon = "bar-chart"}),
}

-- =====================================================
-- TAB FARM
-- =====================================================
Tabs.Farm:AddSection("Auto Farm")

Tabs.Farm:AddToggle("FarmToggle", {
    Title       = "Auto Farm",
    Description = "Tu danh mob + bay dan tiep theo khi het",
    Default     = false,
}):OnChanged(function(v)
    State.farm = v
    if not v and State.active then resetChar() end
    if v and State.quest then task.spawn(acceptQuest) end
end)

Tabs.Farm:AddSlider("ScanRange", {
    Title    = "Scan Range",
    Default  = 2000, Min = 200, Max = 8000, Rounding = 100,
}):OnChanged(function(v) Cfg.scanRange = v end)

Tabs.Farm:AddSlider("AttackRange", {
    Title    = "Attack Range",
    Default  = 200, Min = 50, Max = 500, Rounding = 10,
}):OnChanged(function(v) Cfg.attackRange = v end)

Tabs.Farm:AddButton({
    Title    = "Bay Dan Tiep Theo",
    Callback = function()
        local _, r = getChar()
        if not r then return end
        local nextMob, dist = findMobs(r.Position, Cfg.scanRange)
        if nextMob then
            local er = nextMob:FindFirstChild("HumanoidRootPart")
            if er then
                task.spawn(function()
                    State.active = false
                    flyTo(er.Position + Vector3.new(0,10,0), 300)
                    State.active = true
                    State.target = nextMob
                    startNoFall(nextMob)
                end)
            end
        end
    end,
})

-- =====================================================
-- TAB MOB
-- =====================================================
Tabs.Mob:AddSection("Bring Mob")

Tabs.Mob:AddToggle("BringToggle", {
    Title       = "Auto Bring Mob",
    Default     = false,
}):OnChanged(function(v) State.bring = v end)

Tabs.Mob:AddSlider("BringRange", {
    Title    = "Bring Range",
    Default  = 500, Min = 100, Max = 3000, Rounding = 50,
}):OnChanged(function(v) Cfg.bringRange = v end)

Tabs.Mob:AddButton({
    Title    = "Bring Mob Ngay",
    Callback = function() bringMobs() end,
})

-- =====================================================
-- TAB QUEST
-- =====================================================
Tabs.Quest:AddSection("Auto Quest")

Tabs.Quest:AddToggle("QuestToggle", {
    Title       = "Auto Quest",
    Default     = false,
}):OnChanged(function(v)
    State.quest = v
    if v and State.farm then task.spawn(acceptQuest) end
end)

Tabs.Quest:AddSlider("QuestInterval", {
    Title    = "Quest Interval",
    Default  = 30, Min = 10, Max = 120, Rounding = 5,
}):OnChanged(function(v) Cfg.questEvery = v end)

Tabs.Quest:AddButton({
    Title    = "Nhan Quest Ngay",
    Callback = function()
        if State.questBusy then return end
        task.spawn(acceptQuest)
    end,
})

-- =====================================================
-- TAB MISC
-- =====================================================
Tabs.Misc:AddSection("Cai Dat")

Tabs.Misc:AddButton({
    Title    = "Force Reset",
    Callback = function()
        State.farm = false
        State.questBusy = false
        resetChar()
    end,
})

Tabs.Misc:AddParagraph({
    Title   = "Phim tat",
    Content = "RightShift — An/Hien UI\nNut tron goc trai — An/Hien UI",
})

SaveManager:SetLibrary(Fluent)
InterfaceManager:SetLibrary(Fluent)
InterfaceManager:SetFolder("VexzHub")
SaveManager:SetFolder("VexzHub/configs")
SaveManager:BuildConfigSection(Tabs.Misc)
InterfaceManager:BuildInterfaceSection(Tabs.Misc)

-- =====================================================
-- TAB INFO
-- =====================================================
Tabs.Info:AddSection("Thong Tin")

local InfoP = Tabs.Info:AddParagraph({ Title = "Nhan Vat", Content = "Dang tai..." })
local StatP = Tabs.Info:AddParagraph({ Title = "Trang Thai", Content = "..." })

task.spawn(function()
    while true do
        task.wait(2)
        local lv   = getPlayerLevel()
        local data = getDataForLevel(lv)
        pcall(function()
            InfoP:SetContent(
                "Level: "    .. lv            .. "\n" ..
                "Sea: "      .. data.sea      .. "\n" ..
                "Island: "   .. data.island   .. "\n" ..
                "Quest NPC: ".. data.questNPC .. "\n" ..
                "Mob: "      .. data.mobName
            )
            StatP:SetContent(
                "Farm: "    .. (State.farm  and "ON" or "OFF") .. "\n" ..
                "Quest: "   .. (State.quest and "ON" or "OFF") .. "\n" ..
                "Bring: "   .. (State.bring and "ON" or "OFF") .. "\n" ..
                "Target: "  .. (State.target and State.target.Name or "nil")
            )
        end)
    end
end)

-- =====================================================
-- LOAD
-- =====================================================
SaveManager:LoadAutoloadConfig()
Window:SelectTab(1)

-- =====================================================
-- LOGO TRONG TITLE BAR
-- =====================================================
task.spawn(function()
    task.wait(0.5)
    local pGui = LocalPlayer:WaitForChild("PlayerGui")
    for _, gui in ipairs(pGui:GetChildren()) do
        if gui:IsA("ScreenGui") then
            for _, v in ipairs(gui:GetDescendants()) do
                if v:IsA("TextLabel") and v.Name == "Title" then
                    local logo = Instance.new("ImageLabel")
                    logo.Image = "rbxassetid://118274073113294"
                    logo.Size = UDim2.new(0, 24, 0, 24)
                    logo.Position = UDim2.new(0, -30, 0.5, -12)
                    logo.BackgroundTransparency = 1
                    logo.ZIndex = 10
                    logo.Parent = v.Parent
                    break
                end
            end
        end
    end
end)

-- =====================================================
-- FLOATING TOGGLE BUTTON - BO TRÒN TỰ ĐỘNG
-- =====================================================
local BUTTON_SIZE = 50
local BORDER_THICKNESS = 2
local SHADOW_OFFSET = 3
local CLICK_SHRINK = 0.85
local IMAGE_SIZE = 30

local FloatScreenGui = Instance.new("ScreenGui")
FloatScreenGui.Name = "VexzHub_FloatingButton"
FloatScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")
FloatScreenGui.ResetOnSpawn = false
FloatScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

local ShadowFrame = Instance.new("Frame")
ShadowFrame.Name = "Shadow"
ShadowFrame.Size = UDim2.new(0, BUTTON_SIZE, 0, BUTTON_SIZE)
ShadowFrame.Position = UDim2.new(0, 20, 0, 100)
ShadowFrame.BackgroundTransparency = 1
ShadowFrame.BorderSizePixel = 0
ShadowFrame.ZIndex = 1
ShadowFrame.Parent = FloatScreenGui

local Shadow = Instance.new("ImageLabel")
Shadow.Name = "ShadowImage"
Shadow.Size = UDim2.new(1, SHADOW_OFFSET * 2, 1, SHADOW_OFFSET * 2)
Shadow.Position = UDim2.new(0, SHADOW_OFFSET, 0, SHADOW_OFFSET)
Shadow.BackgroundTransparency = 1
Shadow.Image = "rbxassetid://118274073113294"
Shadow.ImageColor3 = Color3.new(0, 0, 0)
Shadow.ImageTransparency = 0.5
Shadow.ScaleType = Enum.ScaleType.Fit
Shadow.ZIndex = 1
Shadow.Parent = ShadowFrame

local ShadowCorner = Instance.new("UICorner")
ShadowCorner.CornerRadius = UDim.new(0.5, 0)
ShadowCorner.Parent = Shadow

local ButtonFrame = Instance.new("Frame")
ButtonFrame.Name = "ButtonFrame"
ButtonFrame.Size = UDim2.new(1, -SHADOW_OFFSET * 2, 1, -SHADOW_OFFSET * 2)
ButtonFrame.Position = UDim2.new(0, 0, 0, 0)
ButtonFrame.BackgroundColor3 = Color3.fromRGB(255, 105, 180)
ButtonFrame.BorderSizePixel = 0
ButtonFrame.ZIndex = 2
ButtonFrame.Parent = ShadowFrame

local ButtonCorner = Instance.new("UICorner")
ButtonCorner.CornerRadius = UDim.new(0.5, 0)
ButtonCorner.Parent = ButtonFrame

local UIStroke = Instance.new("UIStroke")
UIStroke.Thickness = BORDER_THICKNESS
UIStroke.Color = Color3.fromRGB(255, 255, 255)
UIStroke.Transparency = 0.3
UIStroke.Parent = ButtonFrame

local ButtonImage = Instance.new("ImageLabel")
ButtonImage.Name = "ButtonImage"
ButtonImage.Size = UDim2.new(0, IMAGE_SIZE, 0, IMAGE_SIZE)
ButtonImage.Position = UDim2.new(0.5, -IMAGE_SIZE/2, 0.5, -IMAGE_SIZE/2)
ButtonImage.BackgroundTransparency = 1
ButtonImage.Image = "rbxassetid://118274073113294"
ButtonImage.ScaleType = Enum.ScaleType.Fit
ButtonImage.ZIndex = 3
ButtonImage.Parent = ButtonFrame

local ImageCorner = Instance.new("UICorner")
ImageCorner.CornerRadius = UDim.new(1, 0)
ImageCorner.Parent = ButtonImage

-- =====================================================
-- FLOATING BUTTON LOGIC
-- =====================================================
local isDragging = false
local dragStart = nil
local startPos = nil
local dragThreshold = 5
local hasMoved = false

local function playClickEffect()
    local tween = TweenService:Create(
        ButtonFrame,
        TweenInfo.new(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
        { Size = UDim2.new(CLICK_SHRINK, -SHADOW_OFFSET * 2, CLICK_SHRINK, -SHADOW_OFFSET * 2) }
    )
    tween:Play()
    tween.Completed:Wait()
    
    local tweenBack = TweenService:Create(
        ButtonFrame,
        TweenInfo.new(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
        { Size = UDim2.new(1, -SHADOW_OFFSET * 2, 1, -SHADOW_OFFSET * 2) }
    )
    tweenBack:Play()
end

local function playHoverEffect(enter)
    local targetTransparency = enter and 0.2 or 0.5
    local tween = TweenService:Create(
        Shadow,
        TweenInfo.new(0.2, Enum.EasingStyle.Quad),
        { ImageTransparency = targetTransparency }
    )
    tween:Play()
end

local function toggleMainUI()
    local pGui = LocalPlayer:WaitForChild("PlayerGui")
    for _, gui in ipairs(pGui:GetChildren()) do
        if gui:IsA("ScreenGui") and gui ~= FloatScreenGui then
            gui.Enabled = not gui.Enabled
        end
    end
end

ButtonFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or
       input.UserInputType == Enum.UserInputType.Touch then
        isDragging = true
        dragStart = input.Position
        startPos = ShadowFrame.Position
        hasMoved = false
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if isDragging and (input.UserInputType == Enum.UserInputType.MouseMovement or
        input.UserInputType == Enum.UserInputType.Touch) then
        local delta = input.Position - dragStart
        if delta.Magnitude > dragThreshold then
            hasMoved = true
            ShadowFrame.Position = UDim2.new(
                0,
                startPos.X.Offset + delta.X,
                0,
                startPos.Y.Offset + delta.Y
            )
        end
    end
end)

UserInputService.InputEnded:Connect(function(input)
    if isDragging and (input.UserInputType == Enum.UserInputType.MouseButton1 or
        input.UserInputType == Enum.UserInputType.Touch) then
        isDragging = false
        if not hasMoved then
            playClickEffect()
            toggleMainUI()
        end
    end
end)

ButtonFrame.MouseEnter:Connect(function()
    if not isDragging then
        playHoverEffect(true)
    end
end)

ButtonFrame.MouseLeave:Connect(function()
    playHoverEffect(false)
end)

RunService.RenderStepped:Connect(function()
    if isDragging then
        local pos = ShadowFrame.Position
        local screenSize = workspace.CurrentCamera.ViewportSize
        
        local x = math.clamp(pos.X.Offset, 0, screenSize.X - BUTTON_SIZE - SHADOW_OFFSET * 2)
        local y = math.clamp(pos.Y.Offset, 0, screenSize.Y - BUTTON_SIZE - SHADOW_OFFSET * 2)
        
        if x ~= pos.X.Offset or y ~= pos.Y.Offset then
            ShadowFrame.Position = UDim2.new(0, x, 0, y)
        end
    end
end)

print("[Vexz Hub v3.0] Loaded!")
print("  - RightShift: An/Hien UI")
print("  - Nut tron goc trai: An/Hien UI + keo tha") 
